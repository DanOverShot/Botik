# Основные зависимости
python-telegram-bot==20.5  # Основная библиотека для работы с Telegram API (версия 20.x+ с асинхронной поддержкой)

# Дополнительные зависимости
python-dotenv==1.0.0       # Для загрузки переменных окружения (рекомендуется для хранения токена)
Pillow==10.0.0             # Для обработки изображений (может потребоваться для анализа мемов)

# Стандартные библиотеки Python (фактически не требуют установки)
# os, sys, time, collections, random, logging
